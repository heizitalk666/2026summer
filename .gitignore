__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# 运行产物
logs/
evidence/
cloud/patrol.db
cloud/storage/
*.jsonl

# 工具产物（viewer / tune_pid / calibrate 的输出）
out/

# 训练产物
training/datasets/
training/runs/
*.pt
*.onnx
*.rknn
cloud/patrol.db-wal
cloud/patrol.db-shm
.coverage
htmlcov/

# PPT 生成的中间产物（node 依赖与渲染出的 PDF/JPG）
deliverables/node_modules/
deliverables/_deck/node_modules/
deliverables/*.pdf
deliverables/slide-*.jpg

# 交付原件的压缩包与解压副本。**内容已装进仓库对应位置**（脚本进 training/、
# 权重进 training/runs/、ONNX 进 artifacts/），压缩包只作本地存档，不进库：
# 丙那两个加起来 85 MB，超过 GitHub 50 MB 的告警线。
# XiaoChe_APM32.zip 是早已跟踪的底盘固件，不受这条影响（gitignore 不会取消跟踪）。
*.7z
deliverables/**/丙-L3*.zip

# 甲/丙/乙 交付里随包附的数据集副本。与 training/datasets/ 同一条政策：
# 数据集不进版本库，位置写在各自的 where.txt 里。
deliverables/乙-分割/train/

# 甲的 ONNX 导出目录（*.onnx 已被上面挡住，这里连空目录一起挡）
artifacts/

# 答辩 PPT 的旧稿。deliverables/中期答辩.pptx 是现行版（9-05），这份是 9-04 的
# 重复下载稿（文件名带 "(7)"），11.5 MB，留本地存档不进库。
deliverables/中期答辩-旧稿*.pptx

# 部署包与上板模型（deploy/build_package.py 生成，不进版本库）
dist/
models/

# 答辩 PPT 骨架：由 final.py stage A 从学院模板生成，30 MB
deliverables/_deck_final/skeleton_final.pptx

# 学院给的模板（不是本项目的产出，31 MB / 几十 KB）。前者是 PPT stage A 的输入，
# 见 deliverables/_deck_final/README.md；后两个是实训报告模板。
C424 PPT模板-2024新.pptx
测控系统综合实训小组报告模板.docx
测控系统综合实训个人报告模板.docx

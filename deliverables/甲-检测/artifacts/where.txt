# 太大不进版本库的产物放哪
#
# 一行一个，格式：<文件名>  <链接或路径>  <大小>  <sha256 前 8 位>

# 两级权重都已在本地工作树里，**不需要再传**。核对过架构：
best_cruise.pt   deliverables/甲-检测/best.pt                 19,162,202 B   b23e04bb   yolo11s
best_verify.pt   deliverables/甲-检测/verify/weights/best.pt  40,509,676 B   b655d34c   yolo11m

# 已复制到 configs/system.yaml 实际读取的位置：
#   perception.detector.weights_cruise → training/runs/cruise/weights/best.pt
#   perception.detector.weights_verify → training/runs/verify/weights/best.pt

# 说明：best.pt 曾被提交到 deliverables/甲-检测/ 根目录（19 MB）。
# .gitignore:20 本来就有 *.pt，但 GitHub 网页版「Add files via upload」
# 不走 .gitignore，所以绕过去了。**它仍在 git 历史里**，想彻底清掉要重写历史，
# 那是共享分支上的破坏性操作，需要全组同意再做。
#
# 以后传大文件走 git 命令行（会被 .gitignore 挡住），别用网页版上传。

---

# 权重齐了，但甲这一路仍缺的是**文本**，不是权重：
#
#   training/gen_yolo_renders.py          不存在。README 的复现命令第一条就是它
#   training/train_detector.py            仓库版没有 --weights / --name 两个参数，
#                                         而 README 的两条命令都用了，照敲会报
#                                         unrecognized arguments
#   cruise_ft / verify_ft 的 stage_meta.json + results.csv
#                                         进库的 stage_meta 是**微调前**那版
#                                         （巡航 0.9949 / 复核 0.9943），
#                                         和一页纸上的数不是同一次训练
#   ultralytics val 的原始输出            单帧耗时 ~15 ms、漏检率 0.17 % 的出处
#
# 这四样不到位，docs/指标汇总表.md 里 L1 那三行就一直是「不可复现」。

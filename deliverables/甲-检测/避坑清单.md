# 避坑清单（工作用，**不进 PPT**）

组长在建目录时顺手核对代码整理的。都不是猜的，每条都能指到行号。

---

## 1. 切到 yolo 那一刻，像素密度的来源变了（最要紧的一条）

`patrol/perception/node.py:413` 的注释说得很清楚，距离有两个来源：

| 检测器 | 距离哪来的 | 性质 |
|---|---|---|
| `synthetic`（现默认） | 检测器直接给 `distance_m`（`detector/synthetic.py:99`） | **场景真值加噪的 oracle**，不是测出来的 |
| `yolo`（你要切的） | 由 bbox 高度反算（`scene/optics.py:distance_from_bbox_height`） | 真·估计值 |

像素密度 `p` 是由距离算的，而 `p` 决定复核变焦倍率、决定 fusion 那条
「密度不达标就不下读数类结论」的门槛（`perception/fusion.py:49`，门槛 96 px）。

**所以切换前后对比表里的密度比、复核成功率如果掉了，第一嫌疑不是检测精度变差，
而是距离从"真值"变成了"估计"。** 这两件事要分开报告，否则会把一个预期内的
系统性变化写成模型不行。

**建议加一列诊断**：从证据包里取 `before.est_distance_m` 与场景真值距离对比，
出一张「距离估计误差」的小表。这一列能把两种原因分开，而且它本身就是
「sim-to-real gap」最直观的证据，答辩好讲。

> 这条路径上原本有个 bug（反算漏传 zoom，复核态 3× 时距离算成真值的 1/3），
> 已在 `4ffb316` 修掉，并由 `tests/test_distance_estimation.py` 钉住。
> 你切过去时它应该已经是对的——但那些测试**不需要权重就能跑**，
> 切换前先跑一遍确认：`pytest tests/test_distance_estimation.py -v`

---

## 2. 「不退化」不能只跑一轮

`run_all` 的复核成功率天然在 **50 %–100 %** 之间浮动
（`docs/新手上路.md:170` 写明是正常的）。组长实测过两轮：**83.3 %** 和 **75.0 %**。

单轮对比分不清退化和噪声。**每边至少三轮，报区间不报单点。**
证据包数同理（实测一轮 4 个、一轮 6 个）。

这和乙 那边踩的是同一个坑：样本量不够时，你比的是噪声不是效果。

---

## 3. 权重从公开集训，不要从合成集训

`docs/后续计划与分工.md:194` 和 `training/README.md` 都写了理由：
**合成数据补的是密度分层，不是纹理多样性。** 拿合成集训的权重上真机，
sim-to-real gap 会把指标吃掉一大截。

合成集的正确用法是**增广**，也就是关键数字表里那张「只用公开集 vs 公开集+合成集」
的对比表——它是任务书要求的方案比选证据，别省。

---

## 4. 模型名枚举是硬的

`patrol/schemas/detection_event.schema.json:74-75` 里 `model.name` 只有两个值：

```
yolo11s   yolo11m
```

Schema 是 `additionalProperties: false` 且这个枚举是冻结的。训了别的骨干
（yolov8s、rtdetr…）要报进 `DetectionEvent`，**必须走 `validate.py` 的
`ALLOWED_DRIFT` 白名单流程**，不许静默塞值——塞了 `validate` 会红，
而 `validate` 不过就不该提交。

---

## 5. 第一天先出 100 张小批次

`docs/后续计划与分工.md:197` 点名的：乙和丙要拿你的数据跑通各自的流程。
**别等全量 2773 张下完再给**，卡住的是两个人。

---

## 6. 提交前的固定动作

```bash
python -m patrol.tools.validate     # 51 项，这条不过就别提交
python -m pytest -q                 # 一条都不许退化
```

---

## 7. mAP50 0.9949 需要一句解释，答辩一定会问

巡航级的实测：

| | epoch 1 | epoch 120 |
|---|---|---|
| mAP50 | **0.9759** | 0.9949 |
| mAP50-95 | 0.6449 | 0.7513 |

**第一轮就 0.976，这个数本身是要被追问的。**「你这个指标为什么这么高」比
「为什么这么低」更难答——评审的下一句一定是「训练集和验证集是不是同一批图」。

先说好消息：**转换脚本没有引入泄漏。** `prepare_dataset.py:131` 沿用
Roboflow 自己的划分（`train→train`，`valid`/`val`/`test→val`），没有重新随机
划分，所以泄漏不是这一步造成的。

那高在哪？两个可查的方向，**建议至少查第一个**：

1. **Roboflow 导出常带增广副本。** 同一张原图经旋转/裁剪/调色生成多张，
   如果这些副本跨了 train/val，验证集里就有训练集的近邻。
   **已经写好命令了，跑一条就行**：

   ```bash
   python -m training.prepare_dataset --check-leak
   ```

   它按 `.rf.` 左边那一截把文件名归回原图（副本的文件名各不相同，
   **光比文件名一个都查不出来**），然后找跨 split 出现的原图，
   并报出「val 有多少比例被牵连」——这个比例决定这批指标还能不能用。

   - 打印 `PASS` → 泄漏排除了，0.9949 可以写进报告，用第 2 条解释
   - 打印 `FAIL` → 按原图重新划分后重训，或在报告里如实写明这个局限

   **无论哪个结果都要把这条命令的输出贴进 `artifacts/`**，
   这是「你这个数怎么来的」最直接的回答。
2. **数据集本身就容易。** `prepare_dataset.py:59` 对这个源的评价原话是
   「场景/光照/视角一致，不需要做域适配」。一致性高的数据集，预训练 YOLO
   本来就能很快打满——这是个合理解释，但**要有第 1 条的排查垫底才敢这么说**。

### 连带的一件事：复核级的验收标准需要改口径

分工书 `:177` 给复核级的标准是「mAP50 优于巡航模型 ≥ 5 个点」。
**巡航级已经 0.9949，只剩 0.5 个点的空间，这条标准在当前数据集上做不到。**

这不是甲的问题，是标准定的时候没预料到巡航级会这么高。建议改成下面之一，
**并在答辩时讲清为什么改**（这本身是个诚实的发现）：

- 改比 **mAP50-95**（现在 0.7513，余量很大，而且它对定位精度更敏感——
  复核级的价值恰恰在于定位更准，好让变焦对得更准）
- 改比**小目标 / 低像素密度子集**上的 mAP。复核级本来就是为「看不清的目标」
  准备的，在全集上比反而稀释了差异
- 或者干脆承认「在这个数据集上两级模型分不出差距」，
  **那也是结论**——和乙、丙 那两路遇到的是同一件事：
  **测试集太容易时，比选结论是被数据决定的，不是被方法决定的。**

第二条最贴合项目立论，建议优先。

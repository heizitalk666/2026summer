# 边缘端 + 云端。约 80 MB，学生笔记本可直接装。
numpy>=1.26
opencv-python>=4.9        # 无显示环境可换 opencv-python-headless
pyzmq>=25.0
pyyaml>=6.0
jsonschema>=4.20
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9
paho-mqtt>=1.6
pyserial>=3.5
requests>=2.31
pytest>=8.0
httpx>=0.27              # fastapi.testclient 需要，tests/test_cloud.py 用

# ---- 识别层可选依赖。装不上时对应通路自动降级，全链路照常跑通 ----
# OCR：第二条读数通路。15 MB 的包里自带 ONNX 权重，装完不联网就能用，
# 且不依赖 torch。这是本项目唯一一个从安装到推理完整可跑的学习模型。
# 可选：不装则 perception.ocr 自动降级为 off。
#
# **不要写成 >=1.3。**rapidocr-onnxruntime 1.3.0–1.3.17 要求 Python <3.12，
# 1.3.18–1.4.4 要求 <3.13，而本项目在 Python 3.13 上开发（miniconda 默认）。
# 也就是说 >=1.3 的**每一个版本**在 3.13 上都装不上——这条约束原先是无解的，
# pip 只会报 "No matching distribution found"，看着像网络问题。
# 1.2.3 是 3.13 上能装的最高版，实测读数能力与 3.x 没有可观察差异
# （见 tests/test_ocr.py 里 70.4 那段说明）。
#
# 后继包 `rapidocr`（3.9.2）支持 3.13，但 API 不兼容、且会把 numpy 顶到 2.x
# 与 opencv 顶到 5.x，与本项目其余依赖冲突。要迁的话是一次单独的工作。
rapidocr-onnxruntime>=1.2.3
onnxruntime>=1.17           # 可选：检测/分割/异常的 ONNX 推理后端
